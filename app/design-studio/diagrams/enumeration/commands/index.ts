// Enumeration commands are now consolidated into generic member commands
// Re-export from the new location for backward compatibility
export {
  AddMemberCommand,
  DeleteMemberCommand,
  UpdateMemberCommand,
  ENUMERATION_LITERAL_CONFIG,
} from '~/core/commands/canvas/members';
